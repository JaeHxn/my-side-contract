# 내편계약서

계약서 AI 분석 서비스 — 일반인이 받은 계약서를 실시간 법령과 대조하여 불리한 조항을 즉시 분석해주는 웹 서비스.

**스택:** Next.js + TypeScript + Tailwind / Supabase / OpenAI API + OCR + 국가법령정보센터 API / Vercel

---

## 하네스: 내편계약서

**목표:** Next.js MVP 개발과 마케팅 콘텐츠 생성을 에이전트 팀으로 자동화한다.

## 제1조 — 하네스 실행 규칙

내편계약서 작업에서는 아래 규칙을 최우선 프로젝트 규칙으로 적용한다.

- 반드시 이 저장소의 하네스 엔지니어링 구조를 따른다.
- 개발 요청은 `.claude/skills/contract-dev` 오케스트레이터 기준으로 라우팅한다.
- MVP 전체 개발이나 여러 영역이 걸친 작업은 `frontend-dev`, `backend-dev`, `ai-integrator`, `qa` 에이전트를 병렬/단계형으로 실행한다.
- 하네스 포인터는 `CLAUDE.md`, 에이전트 정의는 `.claude/agents/`, 스킬 정의는 `.claude/skills/`에 둔다.
- 환경변수는 `.env.local`만 조회한다. 값은 출력하지 않고 키 존재 여부만 확인한다.
- 사용자가 명시적으로 요청하지 않는 한 `.env.example`을 생성하거나 수정하지 않는다.
- 사용자가 "제1조가 뭐야?" 또는 "기억하고 있어?"라고 물으면 이 규칙을 그대로 요약해서 답한다.

**트리거:**
- 개발(페이지/API/기능/버그) 관련 요청 → `contract-dev` 스킬 사용
- 법령 API 코드 관련 요청 → `legal-api` 스킬 사용
- 마케팅 콘텐츠(블로그/커뮤니티/쇼츠) 요청 → `content-marketing` 스킬 사용
- 단순 질문이나 개념 설명은 직접 응답 가능

**변경 이력:**
| 날짜 | 변경 내용 | 대상 | 사유 |
|------|----------|------|------|
| 2026-05-17 | 초기 구성 | 전체 | MVP 개발 시작 |
